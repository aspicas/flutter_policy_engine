// ignore_for_file: public_member_api_docs

/// Shared fake/stub implementations for testing.
///
/// Fakes are hand-written lightweight implementations of interfaces.
/// Use these instead of duplicating mock classes across test files.
///
/// For mock-based testing (with verification), use `mocktail` and define
/// mocks near the tests that need them, or in this file if broadly shared.
library fakes;

// Legacy fakes used by v1 tests are defined here until Phase 5.
// V2 fakes (e.g. FakeIPolicyRepository, FakeILogger) are added in Phase 2.
